import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    alignItems: 'center',
    borderRadius: 30,
    flexDirection: 'row',
    justifyContent: 'center',
    margin: 10,
  },
  buttonText: {
    fontWeight: '700',
    fontSize: 20,
    paddingVertical: 12,
  },
});
